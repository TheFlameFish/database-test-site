# Description
A simple web application using flask and SQLite that will take, store, and display user data.

# Instructions
Place the site directory within another directory dedicated to the application. This directory will host the database. When running the app **run from within the site directory! If you don't do that, the app will not function.**